---
name: tlaps-proof
description: "Machine-check TLA+ proofs with TLAPS (tlapm) — the unbounded, prove-for-all rung of the ladder. Use when: (1) discharging an obligation dispositioned `tlaps:<theorem>` by running/re-checking its proof, (2) maintaining proofs after a spec edit (confirming obligations still discharge), (3) reporting a failed proof obligation. AUTHORING new proofs needs a strong model — this skill scopes a weak model to running and maintaining EXISTING proofs. Drives `tlapm` through host-prove's wrapper so the verdict is one machine-readable line."
---

Read `guide.md` for the full procedure and the author-vs-maintain split.
